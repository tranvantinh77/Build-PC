pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "build_pc"
include ':app'
include ':screens_login'
include ':screens_home'
include ':pc_common'
include ':pc_api'
include ':screens_splash'
